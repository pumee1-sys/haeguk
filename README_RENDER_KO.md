# HAEGUK v472 Render 서버
Build Command: `pip install -r requirements.txt`
Start Command: `python haeguk_relay_server.py`
클라이언트: `wss://haeguk.onrender.com`

GitHub 저장소의 기존 서버 파일들을 이 ZIP 내용으로 교체/덮어쓴 뒤 커밋하면 Render가 자동 재배포합니다.
